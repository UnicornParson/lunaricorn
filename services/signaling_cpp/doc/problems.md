# Problems: Известные проблемы и технический долг

## Критические

### P1: Зависание при ожидании лидера (LeaderConnector)
- **Статус:** ✅ Исправлено 07.07.2026
- **Файл:** `lunaricorn/cpp/leader_api.cpp`, `services/signaling_cpp/app/main.cpp`
- **Проблема:** `is_ready()` использовал кэшированную сессию с таймаутом 30 сек, из-за чего `wait_for_ready(5, 1)` фактически вис по 30 сек на каждой итерации. Отсутствовал `session_->reset()` при ошибках — после первого таймаута Poco сессия оставалась в битом состоянии. В `main.cpp` был лишний `sleep_for(1)` после `wait_for_ready`, что замедляло повторные попытки в 6 раз.
- **Решение:** `is_ready()` переписан на отдельную короткоживущую сессию с таймаутом 2 сек. В `make_request` добавлен `session_->reset()` при любом исключении. Из `main.cpp` убран дублирующий `sleep_for(1)`.
- **Рекомендация:** Следить, что при перезапуске лидера signaling корректно восстанавливает соединение (интеграционный тест).

### P2: Reconnect логика отсутствует
- **Статус:** ✅ Реализовано
- **Файл:** `lunaricorn/cpp/signaling_reconnect.h`, `lunaricorn/cpp/signaling_api.h`, `lunaricorn/cpp/signaling_api.cpp`
- **Влияние:** Решено. Клиент (SignalingConnector) автоматически переподключается с exponential backoff (1s–60s) + jitter. Подписки восстанавливаются после переподключения.
- **Рекомендация:** Добавить интеграционные тесты для reconnect.

### P2: Нет unit-тестов
- **Статус:** ✅ тестируется через CLI
- **Файл:** `test_client/`
- **Влияние:** Любое изменение кода может сломать существующую логику без возможности обнаружить это автоматически.
- **Рекомендация:** Написать unit-тесты для SignalingProto (сериализация/десериализация), MessageStorage (CRUD), RawEndpoint (protocol handling), SignalingEngine (подписки/диспатч).

### P3: processQueryRequest не связан с SignalingEngine
- **Статус:** ✅ Реализовано
- **Файл:** `app/raw_endpoint.cpp::processQueryRequest()`
- **Проблема:** Отправлял ACK, но не выполнял поиск через `engine->findEvents()`.
- **Влияние:** Решено. Клиенты могут запрашивать историю событий через raw-протокол с фильтрацией по timestamp, types, sources, affected, tags и limit.
- **Рекомендация:** Добавить интеграционные тесты для query-запросов.

## Средние

### M1: Docker healthcheck отсутствует
- **Статус:** ❌ Не реализовано
- **Файл:** `services/docker-compose.yaml`, `services/signaling_cpp/Dockerfile`
- **Влияние:** Docker не может автоматически определить, что сервис запущен и готов принимать соединения.
- **Рекомендация:** Добавить healthcheck через `GET /health` HTTP endpoint (curl или аналогичный скрипт).

### M2: Нет README.md
- **Статус:** ❌ Не создан
- **Файл:** `services/signaling_cpp/README.md`
- **Влияние:** Разработчики не имеют инструкций по сборке, настройке и запуску.
- **Рекомендация:** Создать README.md с разделами: prerequisites, build, configuration (переменные окружения), run, docker, testing.

### M3: STATUS.md устарел
- **Статус:** ⚠️ Требует обновления
- **Влияние:** Глобальный статус проекта не отражает текущее состояние signaling_cpp.
- **Рекомендация:** Обновить корневой `doc/STATUS.md`.

## Низкие

### L1: IT-скрипт требует доработки
- **Статус:** ⚠️ Существует, но не завершён
- **Файл:** `it.sh`
- **Влияние:** Нет автоматизированной проверки end-to-end сценариев.
- **Рекомендация:** Доработать для запуска сервера + интеграционных тестов через CLI клиент + HTTP тестер.

### L2: TypeHandler<EventDataExtended> — мёртвый код
- **Статус:** ⚠️ Не используется
- **Файл:** `app/event_data_extended_type_handler.h`
- **Влияние:** Нет влияния на работу — MessageStorage использует soci напрямую.
- **Рекомендация:** Удалить или подключить к MessageStorage.

### L3: Telemetry не логируется в файл
- **Статус:** ⚠️ Опционально
- **Влияние:** Отчёт телеметрии выводится только через MLOG_D.
- **Рекомендация:** Добавить опциональное логирование в файл для долгосрочного мониторинга.

---

*Создано: 03.07.2026*
*Обновлено: 04.07.2026 — актуализирован список проблем, удалены решённые*