# 1
Добавить в engine функции перечисления (list_meta и list_data) для внутреннего использования

# 2 
Добавить в engine проверку связности. функция возвращает структуру connectivity_report содержащей std:map в котором ключом является id проблемного объекта а значением строкое описание проблемы.
правила связности:
* если в мета объекте установлен флаг наличия контента, то в базе должен существовать дата объект с тем же id
* если поля связей     std::optional<oid> parent,  std::optional<oid> prev; std::optional<oid> next; содержат значения, соотвествующий объект должен присутствовать в таблице мета объектов

# 